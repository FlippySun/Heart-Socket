name: 🐛 Bug 报告
description: 报告一个 Bug 以帮助我们改进
title: "[Bug]: "
labels: ["bug"]
body:
  - type: markdown
    attributes:
      value: |
        感谢你花时间提交 Bug 报告！请尽可能详细地填写以下信息。

  - type: input
    id: vscode-version
    attributes:
      label: VS Code 版本
      description: "可通过 Help > About 查看"
      placeholder: "例如: 1.96.0"
    validations:
      required: true

  - type: input
    id: extension-version
    attributes:
      label: Heart Socket 版本
      description: "在扩展面板中查看"
      placeholder: "例如: 0.1.1"
    validations:
      required: true

  - type: dropdown
    id: os
    attributes:
      label: 操作系统
      options:
        - macOS
        - Windows
        - Linux
    validations:
      required: true

  - type: dropdown
    id: data-source
    attributes:
      label: 数据源类型
      options:
        - HDS (Health Data Server)
        - Pulsoid
        - HypeRate
        - Custom WebSocket
        - 未连接
    validations:
      required: true

  - type: textarea
    id: description
    attributes:
      label: Bug 描述
      description: 清晰描述遇到的问题
      placeholder: 当我执行某个操作时，期望看到 X，但实际出现了 Y
    validations:
      required: true

  - type: textarea
    id: steps
    attributes:
      label: 复现步骤
      description: 详细描述如何复现这个问题
      value: |
        1. 
        2. 
        3. 
    validations:
      required: true

  - type: textarea
    id: logs
    attributes:
      label: 错误日志
      description: "请运行命令 `Heart Socket: Show Logs` 并粘贴相关日志"
      render: shell
    validations:
      required: false

  - type: textarea
    id: additional
    attributes:
      label: 补充信息
      description: 任何其他可能有帮助的信息（截图、配置等）
    validations:
      required: false
